
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Birthday Universe – Section 1</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body {
      height: 100%;
      width: 100%;
      background: black;
      font-family: 'Segoe UI', sans-serif;
      color: white;
      overflow-y: auto; /* ✅ Scroll enabled */
    }

    .moon-section {
      min-height: 100vh; /* ✅ To make scrollable */
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      text-align: center;
      padding: 60px 20px 80px;
      position: relative;
      z-index: 1;
    }

    .moon-image {
      width: 220px;
      height: 220px;
      animation: glow 3s infinite ease-in-out;
      margin-bottom: 30px;
      z-index: 2;
    }

    @keyframes glow {
      0%, 100% { filter: drop-shadow(0 0 10px pink); }
      50% { filter: drop-shadow(0 0 25px deeppink); }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes heartbeat {
      0% { transform: scale(1); }
      25% { transform: scale(1.08); }
      50% { transform: scale(1); }
      75% { transform: scale(1.08); }
      100% { transform: scale(1); }
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      animation: fadeIn 2s ease forwards;
      text-shadow: 0 0 10px #fff8;
      z-index: 2;
    }

    #panchang {
      font-size: 1.2rem;
      font-family: 'Courier New', monospace;
      white-space: pre-line;
      color: #ffcccc;
      margin-top: 10px;
      animation: fadeIn 3s ease forwards;
      z-index: 2;
    }

    button {
      margin-top: 40px;
      padding: 12px 28px;
      background: hotpink;
      border: none;
      border-radius: 12px;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      color: white;
      box-shadow: 0 0 20px hotpink;
      animation: heartbeat 2s infinite ease-in-out;
      z-index: 2;
    }

    /* Sparkles */
    .glitter-container {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 0;
      height: 0;
      z-index: 0;
      pointer-events: none;
    }

    .glitter {
      position: absolute;
      width: 6px;
      height: 6px;
      background: white;
      border-radius: 50%;
      opacity: 0;
      animation: sparkle 2s ease-out forwards;
    }

    @keyframes sparkle {
      0% { opacity: 1; transform: translateY(0) scale(1); }
      100% { opacity: 0; transform: translateY(-120px) scale(0.3); }
    }

    /* Floating hearts */
    .floating-heart {
      position: absolute;
      width: 14px;
      height: 14px;
      background: pink;
      border-radius: 50%;
      top: 100%;
      animation: floatHeart 6s linear infinite;
      opacity: 0.5;
    }

    @keyframes floatHeart {
      0% { transform: translateY(0) scale(1); }
      100% { transform: translateY(-120vh) scale(1.4); }
    }
  </style>
</head>
<body>

  <!-- Romantic background music -->
  <audio autoplay loop style="display:none">
    <source src="https://cdn.pixabay.com/download/audio/2022/03/15/audio_d4cf5c6507.mp3?filename=romantic-piano-melody-20694.mp3" type="audio/mpeg">
  </audio>

  <section class="moon-section">
    <!-- Glitter -->
    <div class="glitter-container" id="glitterContainer"></div>

    <!-- Moon -->
    <img src="moon.jpeg" alt="moon" class="moon-image">

    <!-- Heading -->
    <h1>7 July 2007</h1>

    <!-- Typing Panchang Style -->
    <div id="panchang"></div>

    <!-- Button with updated pop-up -->
    <button onclick="alert('Now order Agrim for second site 🙇🏻‍♂️')">
      Enter My Universe
    </button>

    <!-- Floating hearts -->
    <script>
      const moonSection = document.querySelector(".moon-section");
      for (let i = 0; i < 25; i++) {
        const heart = document.createElement("div");
        heart.classList.add("floating-heart");
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.animationDelay = Math.random() * 5 + "s";
        heart.style.background = `hsl(${Math.random() * 360}, 100%, 75%)`;
        moonSection.appendChild(heart);
      }
    </script>
  </section>

  <!-- Glitter particles -->
  <script>
    const glitterContainer = document.getElementById('glitterContainer');
    for (let i = 0; i < 70; i++) {
      const glitter = document.createElement('div');
      glitter.classList.add('glitter');
      glitter.style.left = `${Math.random() * 200 - 100}px`;
      glitter.style.top = `${Math.random() * 200 - 100}px`;
      glitter.style.animationDelay = `${Math.random()}s`;
      glitter.style.background = `hsl(${Math.random() * 360}, 100%, 85%)`;
      glitterContainer.appendChild(glitter);
    }
    setTimeout(() => { glitterContainer.remove(); }, 3500);
  </script>

  <!-- Typing Panchang + Birthday Lines -->
  <script>
    const lines = [
      "Date      : 7 July 2007",
      "Day       : Saturday",
      "Time      : 1:40 PM",
      "Paksha    : Krishna",
      "Tithi     : Ashtami",
      "Nakshatra : Uttara Bhadrapada",
      "Yog       : Atiganda",
      "",
      "Iss din janmi thi meri kokil kanthi, sbse pyaari,",
      "sbse sundar, sbse samjhdar, meri zindagi ka pyaar,",
      "mera poora dil,",
      "Miss Akanksha Pandey 💖",
      "Happyyy birthdayyyyyyyy 🎉🎉🎉"
    ];

    const panchangEl = document.getElementById("panchang");
    let currentLine = 0;

    function typeLine(lineIndex, charIndex = 0) {
      if (lineIndex >= lines.length) return;
      const line = lines[lineIndex];
      if (charIndex < line.length) {
        panchangEl.innerHTML += line[charIndex];
        setTimeout(() => typeLine(lineIndex, charIndex + 1), 50);
      } else {
        panchangEl.innerHTML += "\n";
        setTimeout(() => typeLine(lineIndex + 1, 0), 300);
      }
    }

    typeLine(0);
  </script>

</body>
</html>
